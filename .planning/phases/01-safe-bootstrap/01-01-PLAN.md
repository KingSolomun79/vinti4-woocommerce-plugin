---
phase: 01-safe-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vinti4.php
  - includes/class-vinti4-admin-notices.php
  - includes/class-wc-gateway-vinti4.php
  - uninstall.php
  - readme.txt
autonomous: true

must_haves:
  truths:
    - "Activating the plugin with WooCommerce active produces no fatal error"
    - "Activating without WooCommerce shows an admin notice and plugin stays dormant"
    - "Vinti4 appears in the WooCommerce payment methods list"
    - "Deactivating the plugin does not delete any orders, pages, or posts"
  artifacts:
    - path: "vinti4.php"
      provides: "Plugin bootstrap with dependency guards and gateway registration"
      contains: "woocommerce_payment_gateways"
    - path: "includes/class-vinti4-admin-notices.php"
      provides: "Admin notice for missing WooCommerce"
      contains: "register_missing_wc_notice"
    - path: "includes/class-wc-gateway-vinti4.php"
      provides: "Minimal gateway class extending WC_Payment_Gateway"
      contains: "class WC_Gateway_Vinti4"
    - path: "uninstall.php"
      provides: "Safe uninstall with no destructive behavior"
      contains: "ABSPATH"
    - path: "readme.txt"
      provides: "WordPress plugin readme"
      contains: "Vinti4 for WooCommerce"
  key_links:
    - from: "vinti4.php"
      to: "includes/class-vinti4-admin-notices.php"
      via: "require_once outside plugins_loaded"
      pattern: "require_once.*class-vinti4-admin-notices"
    - from: "vinti4.php"
      to: "includes/class-wc-gateway-vinti4.php"
      via: "require_once inside plugins_loaded guard"
      pattern: "require_once.*class-wc-gateway-vinti4"
    - from: "vinti4.php"
      to: "woocommerce_payment_gateways filter"
      via: "add_filter registration with class_exists guard"
      pattern: "add_filter.*woocommerce_payment_gateways"
---

<objective>
Create the complete plugin bootstrap that activates safely on modern WooCommerce with dependency guards, proper gateway registration, admin notices, and safe uninstall behavior.

Purpose: This is the structural foundation for the entire plugin. Every subsequent phase builds on these files. The bootstrap must follow WooCommerce best practices — guarded loading, filter-based gateway registration (no direct instantiation), and graceful degradation when WooCommerce is absent.

Output: A WordPress-plugin-activatable structure with 5 files that passes all 4 Phase 1 success criteria.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/prd.md

PRD sections directly relevant:
- Section 10.4: Gateway ID must be string `vinti4`
- Section 11: Proposed repository tree (file structure)
- Section 12: Files to create — vinti4.php, class-wc-gateway-vinti4.php, class-vinti4-admin-notices.php
- Section 14, Milestone 1: Complete bootstrap code skeleton (lines 460-533)
- Section 15.1: Dependency guard pattern — class_exists('WooCommerce') && class_exists('WC_Payment_Gateway')
- Section 15.2: Do not directly instantiate gateway — register via filter, let Woo instantiate
- Section 15.3: No raw SQL in uninstall
- Section 15.4: No page creation on activation

Legacy problems to AVOID:
- Gateway ID was numeric `2424` — must be string `vinti4`
- No `class_exists('WC_Payment_Gateway')` guard — must add
- `new WC_Gateway_vinti4()` called directly — must NOT do this
- Creates pages on activation — must NOT do this
- Deletes posts on deactivation via raw SQL — must NOT do this
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin bootstrap and admin notices</name>
  <files>vinti4.php, includes/class-vinti4-admin-notices.php</files>
  <action>
Create `vinti4.php` — the main plugin file. Follow the PRD Milestone 1 skeleton (lines 483-526 of prd.md) exactly:

1. **Plugin header**: Plugin Name: "Vinti4 for WooCommerce", Version: 1.0.0, requires at least WordPress 6.0, tested up to 6.8, requires PHP 8.1, license GPLv3.

2. **Constants**: `VINTI4_VERSION`, `VINTI4_PLUGIN_FILE` (__FILE__), `VINTI4_PLUGIN_DIR` (plugin_dir_path), `VINTI4_PLUGIN_URL` (plugin_dir_url).

3. **ABSPATH guard**: `defined( 'ABSPATH' ) || exit;`

4. **Load admin notices unconditionally** (outside plugins_loaded): `require_once VINTI4_PLUGIN_DIR . 'includes/class-vinti4-admin-notices.php';`

5. **plugins_loaded hook (priority 20)**: Check `class_exists('WooCommerce') && class_exists('WC_Payment_Gateway')`. If missing, call `Vinti4_Admin_Notices::register_missing_wc_notice()` and return. If present, require all include files:
   - `includes/functions-vinti4-formatting.php` (do NOT create yet — just add the require_once, the file will be created in Phase 3)
   - `includes/class-vinti4-logger.php` (do NOT create yet — Phase 7, just add require_once)
   - `includes/class-vinti4-fingerprint.php` (Phase 3, just require_once)
   - `includes/class-vinti4-request-builder.php` (Phase 3, just require_once)
   - `includes/class-wc-gateway-vinti4.php` (CREATE in Task 2)
   - `includes/class-wc-vinti4-blocks-support.php` (Phase 6, just require_once)

   IMPORTANT: Only require files that exist. For files not yet created in this phase, COMMENT OUT the require_once with a note like `// Phase 3: require_once ...`. Only `class-vinti4-admin-notices.php` and `class-wc-gateway-vinti4.php` should be actively required in this phase.

6. **woocommerce_payment_gateways filter**: Add filter that checks `class_exists('WC_Gateway_Vinti4')` and appends `'WC_Gateway_Vinti4'` to the methods array.

7. **woocommerce_blocks_payment_method_type_registration action**: Check `class_exists('WC_Vinti4_Blocks_Support')` before registering. COMMENT OUT with note `// Phase 6: Block support registration`.

8. **Text domain**: Add `add_action( 'plugins_loaded', ... )` for `load_plugin_textdomain( 'vinti4', false, dirname( plugin_basename( __FILE__ ) ) . '/languages/' );` — but only as a comment/placeholder for now.

Create `includes/class-vinti4-admin-notices.php`:
- Class `Vinti4_Admin_Notices` with single static method `register_missing_wc_notice()`
- Uses `admin_notices` action hook to render a dismissible error notice
- Notice text: "Vinti4 for WooCommerce requires WooCommerce to be installed and active."
- The notice should be a `notice-error` with `is-dismissible` class
- Include a check so the notice only shows on appropriate admin pages (not every admin page load forever — check if WooCommerce is still missing before rendering)

Both files must use WordPress coding standards:
- Spaces inside parentheses, not tabs for indentation (use tabs actually — WP standard uses tabs)
- Yoda conditions
- Proper i18n with `__()` and `'vinti4'` text domain
- `defined( 'ABSPATH' ) || exit;` at top of included files
  </action>
  <verify>
    1. `php -l vinti4.php` — no syntax errors
    2. `php -l includes/class-vinti4-admin-notices.php` — no syntax errors
    3. `vinti4.php` contains `class_exists( 'WooCommerce' )` and `class_exists( 'WC_Payment_Gateway' )`
    4. `vinti4.php` does NOT contain `new WC_Gateway_Vinti4` (no direct instantiation)
    5. `vinti4.php` contains `add_filter( 'woocommerce_payment_gateways'` (filter-based registration)
    6. `vinti4.php` does NOT contain `register_activation_hook` or page creation
    7. `includes/class-vinti4-admin-notices.php` contains `register_missing_wc_notice` method
    8. Gateway ID string `vinti4` is used (not numeric)
  </verify>
  <done>
    - vinti4.php passes PHP lint check with no syntax errors
    - Dependency guards prevent loading when WooCommerce is absent
    - Gateway registered via woocommerce_payment_gateways filter, not direct instantiation
    - Admin notice class exists and can render missing-WooCommerce warning
    - No page creation, no raw SQL, no activation hooks with destructive behavior
  </done>
</task>

<task type="auto">
  <name>Task 2: Create minimal gateway class, uninstall, and readme</name>
  <files>includes/class-wc-gateway-vinti4.php, uninstall.php, readme.txt</files>
  <action>
Create `includes/class-wc-gateway-vinti4.php` — a minimal but functional WooCommerce gateway class:

1. **ABSPATH guard** at top.

2. **Class `WC_Gateway_Vinti4` extends `WC_Payment_Gateway`**.

3. **Constructor**:
   - `$this->id = 'vinti4';` (string, NOT numeric)
   - `$this->method_title = __( 'Vinti4', 'vinti4' );`
   - `$this->method_description = __( 'Pay via Vinti4 / SISP hosted payment page.', 'vinti4' );`
   - `$this->has_fields = false;`
   - `$this->supports = array( 'products' );`
   - `$this->icon = ''` (empty for now — logo added later)

   Then call:
   - `$this->init_form_fields();`
   - `$this->init_settings();`

   Then load settings:
   - `$this->title = $this->get_option( 'title' );`
   - `$this->description = $this->get_option( 'description' );`
   - `$this->enabled = $this->get_option( 'enabled' );`

   Then add admin options save hook:
   - `add_action( 'woocommerce_update_options_payment_gateways_' . $this->id, array( $this, 'process_admin_options' ) );`

   Then add callback route hook (commented out with Phase 5 note):
   - `// Phase 5: add_action( 'woocommerce_api_' . $this->id, array( $this, 'handle_callback' ) );`

4. **`init_form_fields()`** — minimal settings for Phase 1 (will be expanded in Phase 2):
   ```php
   $this->form_fields = array(
       'enabled' => array(
           'title'   => __( 'Enable/Disable', 'vinti4' ),
           'type'    => 'checkbox',
           'label'   => __( 'Enable Vinti4', 'vinti4' ),
           'default' => 'no',
       ),
       'title' => array(
           'title'   => __( 'Title', 'vinti4' ),
           'type'    => 'text',
           'default' => __( 'Pay with Vinti4', 'vinti4' ),
       ),
       'description' => array(
           'title'   => __( 'Description', 'vinti4' ),
           'type'    => 'textarea',
           'default' => __( 'You will be redirected to Vinti4 to complete payment.', 'vinti4' ),
       ),
   );
   ```
   Phase 2 will add: pos_id, pos_auth_code, vbv2_url, language, debug, currency_default.

5. **`process_payment( $order_id )`** — stub that returns an array for now:
   ```php
   public function process_payment( $order_id ) {
       // Phase 4: Implement full payment redirect flow.
       return array(
           'result'   => 'failure',
           'redirect' => '',
       );
   }
   ```

Create `uninstall.php`:
1. **ABSPATH guard**: `defined( 'WP_UNINSTALL_PLUGIN' ) || exit;`
2. Only delete the gateway settings option: `delete_option( 'woocommerce_vinti4_settings' );`
3. NO raw SQL, NO page deletion, NO post deletion, NO order deletion
4. Add a comment explaining what is and isn't deleted

Create `readme.txt`:
1. Standard WordPress plugin readme format
2. Plugin name: Vinti4 for WooCommerce
3. Contributors: vinti4
4. Tags: vinti4, payment, sisp, woocommerce, cape verde
5. Requires at least: 6.0
6. Tested up to: 6.8
7. Requires PHP: 8.1
8. Stable tag: 1.0.0
9. License: GPLv3
10. Short description and basic installation instructions
11. Changelog section with 1.0.0 entry
  </action>
  <verify>
    1. `php -l includes/class-wc-gateway-vinti4.php` — no syntax errors
    2. `php -l uninstall.php` — no syntax errors
    3. Gateway class has `public $id = 'vinti4'` or sets `$this->id = 'vinti4'` (string ID)
    4. Gateway class extends `WC_Payment_Gateway`
    5. Gateway class has `init_form_fields()` and `init_settings()` calls in constructor
    6. Gateway class has `process_payment()` method (stub is fine)
    7. `uninstall.php` contains `defined( 'WP_UNINSTALL_PLUGIN' )` guard
    8. `uninstall.php` does NOT contain `DELETE`, `wp_delete_post`, `wp_delete_page`, or any destructive operations beyond `delete_option`
    9. `readme.txt` exists with plugin header metadata
    10. No `new WC_Gateway_Vinti4()` anywhere in these files
  </verify>
  <done>
    - Gateway class passes PHP lint, extends WC_Payment_Gateway, uses string ID 'vinti4'
    - Constructor initializes minimal form fields and settings
    - process_payment() stub exists (returns failure — real implementation in Phase 4)
    - uninstall.php only deletes plugin options, never orders/pages/posts
    - readme.txt has valid WordPress plugin readme format
    - All files follow WordPress coding standards
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `php -l` on all 5 created files — zero errors
2. Verify no `new WC_Gateway_Vinti4()` anywhere (only filter registration)
3. Verify no `register_activation_hook` or page creation anywhere
4. Verify no `DELETE FROM` or `wp_delete_post` anywhere
5. Verify gateway ID is string `vinti4` not numeric
6. Verify ABSPATH guards on all PHP files
7. Verify the plugin file structure matches PRD section 11 proposed tree (for the files created in this phase)
</verification>

<success_criteria>
1. All 5 files pass PHP syntax check (`php -l`)
2. Plugin bootstrap has dependency guards (class_exists checks for WooCommerce and WC_Payment_Gateway)
3. Gateway registered via `woocommerce_payment_gateways` filter, not direct instantiation
4. Admin notice class renders warning when WooCommerce is missing
5. Gateway class extends WC_Payment_Gateway with string ID `vinti4`
6. Uninstall is safe — only deletes `woocommerce_vinti4_settings` option
7. No raw SQL, no page creation, no destructive activation/deactivation behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-safe-bootstrap/01-01-SUMMARY.md`
</output>
