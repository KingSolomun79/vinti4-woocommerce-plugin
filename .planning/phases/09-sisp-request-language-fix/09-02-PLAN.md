---
phase: 09-sisp-request-language-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - includes/class-vinti4-redirect-form.php
  - tests/bootstrap.php
  - tests/Test_Request_Builder.php
  - tests/Test_Redirect_Form.php
autonomous: true

must_haves:
  truths:
    - "The hosted redirect page submits the exact browser-safe SISP fields expected by the middleware"
    - "The outbound request includes `languageMessages` and the middleware callback URL so checkout no longer fails on the missing-language contract error"
    - "The shopper browser never receives raw `posAuthCode` in the redirect markup"
    - "Automated tests fail if the request builder or redirect form regresses on field names, casing, or secret exposure"
  artifacts:
    - path: "includes/class-vinti4-redirect-form.php"
      provides: "Auto-submit redirect page using canonical order meta and query-param contract"
      contains: "languageMessages"
    - path: "tests/Test_Request_Builder.php"
      provides: "Unit coverage for expanded payment-attempt contract"
      contains: "build_payment_attempt"
    - path: "tests/Test_Redirect_Form.php"
      provides: "Regression coverage for rendered SISP form payload"
      contains: "Vinti4_Redirect_Form"
  key_links:
    - from: "includes/class-vinti4-redirect-form.php"
      to: "includes/class-wc-gateway-vinti4.php"
      via: "reads `_vinti4_*` request-contract meta stored by process_payment"
      pattern: "_vinti4_"
    - from: "tests/Test_Redirect_Form.php"
      to: "includes/class-vinti4-redirect-form.php"
      via: "asserts rendered HTML/query string matches the middleware contract and omits posAuthCode"
      pattern: "posAuthCode"
---

<objective>
Switch the browser redirect to the corrected SISP transport shape and lock the behavior down with automated regression coverage.

Purpose: Phase 9 is only complete if the rendered request matches the canonical attempt data, sends the required middleware fields, and stops leaking secrets client-side. This plan turns the new server-side contract into the actual redirect payload and proves it with tests.

Output: Updated redirect renderer plus PHPUnit coverage for request-builder output and redirect-form markup/query parameters.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payment-redirect-flow/04-02-SUMMARY.md
@.planning/phases/09-sisp-request-language-fix/09-01-SUMMARY.md
@includes/class-vinti4-redirect-form.php
@includes/class-vinti4-request-builder.php
@tests/bootstrap.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the corrected SISP browser request from stored canonical meta</name>
  <files>includes/class-vinti4-redirect-form.php</files>
  <action>Refactor `Vinti4_Redirect_Form::render()` so it reads the expanded `_vinti4_*` attempt meta from Plan 09-01 and emits the hosted request in the contract shape expected by the SISP middleware.

Use browser-safe hidden inputs for the contract fields that belong in the POST body, including `languageMessages`, `urlMerchantResponse`, `is3DSec`, and the existing business identifiers. Move `FingerPrint`, `TimeStamp`, and `FingerPrintVersion` into the action URL query string if that is the integration pattern already called out by the phase notes; otherwise use the exact transport location verified from the local legacy reference while keeping the canonical field names intact.

Remove the old `lang` field and do not render `posAuthCode` anywhere in the HTML. Keep the standalone page, noscript button, order/key validation, and auto-submit behavior unchanged unless a transport fix requires a small adjustment.</action>
  <verify>`php -l includes/class-vinti4-redirect-form.php` succeeds; code review of rendered field names shows `languageMessages` present and `posAuthCode` absent</verify>
  <done>The redirect page submits only the corrected browser-safe contract fields, with exact field names/casing and no raw auth secret in markup</done>
</task>

<task type="auto">
  <name>Task 2: Add PHPUnit coverage for attempt-building and rendered redirect payload</name>
  <files>tests/bootstrap.php, tests/Test_Request_Builder.php, tests/Test_Redirect_Form.php</files>
  <action>Extend the pure-PHP test bootstrap only as needed to instantiate the gateway, stub locale helpers, stub `WC()` / payment gateway lookup, and capture redirect-form output safely.

Create `tests/Test_Request_Builder.php` to verify `build_payment_attempt()` returns the new contract fields, uses the expected language fallback behavior, and excludes raw `posAuthCode` from the returned attempt array.

Create `tests/Test_Redirect_Form.php` to render the HTML for a mocked order and assert all of the following: required hidden inputs exist with exact names, the action URL carries the required query parameters when applicable, `urlMerchantResponse` points to the WooCommerce callback endpoint, the exact `_vinti4_*` keys persisted by `process_payment()` in Plan 09-01 are the ones consumed by `Vinti4_Redirect_Form::render()`, and the HTML does not contain `name="posAuthCode"` or the raw auth secret value.

Prefer focused assertions over snapshot testing so failures clearly show which field regressed.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Request_Builder|Test_Redirect_Form"` passes, including assertions that the `_vinti4_*` handoff contract from `process_payment()` matches what `Vinti4_Redirect_Form::render()` consumes</verify>
  <done>Automated tests cover the expanded attempt payload and the rendered redirect form, including a regression assertion that the persisted `_vinti4_*` handoff contract matches render-time consumption and that the browser payload never contains `posAuthCode`</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase-level automated regression checks</name>
  <files>includes/class-vinti4-redirect-form.php, tests/Test_Request_Builder.php, tests/Test_Redirect_Form.php</files>
  <action>Run the smallest relevant verification loop after Tasks 1-2: targeted PHPUnit first, then the full PHPUnit suite if the targeted tests pass. Fix any test bootstrap drift introduced by the new redirect-rendering assumptions before concluding the plan.

Do not leave phase-critical tests skipped. If an assertion must change because the transport moved from hidden fields to query params, update the tests to check the real contract rather than weakening them.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Request_Builder|Test_Redirect_Form"` passes, then `vendor/bin/phpunit` passes</verify>
  <done>Phase 9 request-shape behavior is protected by automated tests and the existing PHPUnit suite still passes after the redirect-form refactor</done>
</task>

</tasks>

<verification>
1. The redirect HTML contains `languageMessages`, `urlMerchantResponse`, and `is3DSec`
2. The redirect transport uses the correct query-param/body split for fingerprint, timestamp, and fingerprint version
3. `posAuthCode` is absent from both hidden inputs and query string
4. `vendor/bin/phpunit` passes with the new request-shape tests included
</verification>

<success_criteria>
- Browser-rendered request matches the stored canonical contract instead of reassembling fields ad hoc
- The missing-language middleware failure is addressed by sending the correct language field and required request metadata
- Test coverage exists for both the canonical attempt array and the final rendered browser payload
</success_criteria>

<output>
After completion, create `.planning/phases/09-sisp-request-language-fix/09-02-SUMMARY.md`
</output>
