---
phase: 09-sisp-request-language-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A real WooCommerce checkout reaches the SISP middleware without the `languageMessages` required-field error"
    - "The live redirect request no longer exposes raw `posAuthCode` in browser-visible form fields or URL"
    - "The callback return path still points back to the plugin's WooCommerce handler after the request-shape change"
  artifacts:
    - path: "includes/class-vinti4-request-builder.php"
      provides: "Canonical request-contract source used in live checkout"
      contains: "urlMerchantResponse"
    - path: "includes/class-vinti4-redirect-form.php"
      provides: "Live browser redirect payload for SISP checkout"
      contains: "languageMessages"
  key_links:
    - from: "includes/class-vinti4-redirect-form.php"
      to: "SISP hosted middleware request"
      via: "browser POST/action URL observed in DevTools during checkout"
      pattern: "languageMessages"
---

<objective>
Perform the live WordPress/WooCommerce verification pass that proves the Phase 9 fix actually removes the checkout blocker in a real browser flow.

Purpose: This phase exists because production-like testing exposed a real middleware error. Automated tests are necessary but not sufficient; the plan must end with one focused human verification gate against the sandbox checkout flow.

Output: Human-approved verification that the corrected request reaches SISP without the missing-language error and no browser-visible auth secret remains.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sisp-request-language-fix/09-01-SUMMARY.md
@.planning/phases/09-sisp-request-language-fix/09-02-SUMMARY.md
@includes/class-vinti4-request-builder.php
@includes/class-vinti4-redirect-form.php
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Expanded SISP request contract, corrected redirect payload, and automated regression coverage for the Phase 9 checkout blocker</what-built>
  <how-to-verify>
1. In the WordPress sandbox store, enable Vinti4 with the sandbox POS credentials already documented in `.planning/PROJECT.md` and ensure debug logging is on.
2. Place a real test order using Vinti4 from both the classic checkout (and block checkout if available in the local site). When redirected to `/vinti4-payment/`, inspect the generated form/network request in browser DevTools.
3. Confirm the browser-visible request includes `languageMessages`, `urlMerchantResponse`, `is3DSec`, and the expected fingerprint/timestamp/version transport fields; confirm it does not include raw `posAuthCode` in hidden inputs, page source, or the action URL.
4. Let the auto-submit continue to SISP. The previous middleware error `languageMessages é obrigatório para o funcionamento do Middleware` must not appear.
5. Complete or cancel the sandbox flow far enough to confirm the return URL still points to the WooCommerce callback path controlled by the plugin.
6. If anything fails, capture the exact field mismatch or browser-visible payload evidence so the next revision can target the real request delta.</how-to-verify>
  <resume-signal>Type "approved" if the live checkout passes, or reply with the exact mismatch/error observed.</resume-signal>
</task>

</tasks>

<verification>
1. Browser DevTools confirms the corrected outbound request shape
2. The middleware no longer rejects the request for missing `languageMessages`
3. No raw `posAuthCode` is visible anywhere in the browser request
</verification>

<success_criteria>
- Human verification confirms the original live blocker is gone
- The live request shape matches the Phase 9 contract assumptions
- The phase is ready to hand off to Phase 10 without unresolved ambiguity about the outbound SISP payload
</success_criteria>

<output>
After completion, create `.planning/phases/09-sisp-request-language-fix/09-03-SUMMARY.md`
</output>
