---
phase: 09-sisp-request-language-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-vinti4-request-builder.php
  - includes/class-wc-gateway-vinti4.php
autonomous: true

must_haves:
  truths:
    - "A payment attempt stores every browser-safe SISP field the redirect page needs before the shopper leaves checkout"
    - "The persisted attempt data includes the exact `languageMessages` value the middleware validates, without fallback reconstruction during redirect rendering"
    - "The redirect contract can be rendered from stored callback URL, 3DS flag, timestamp, `FingerPrint`, and fingerprint-version data without recomputing those fields"
    - "No browser-safe redirect contract, order meta handoff, or log output exposes raw `posAuthCode`"
  artifacts:
    - path: "includes/class-vinti4-request-builder.php"
      provides: "Canonical attempt builder for SISP middleware fields"
      contains: "FingerPrint"
    - path: "includes/class-wc-gateway-vinti4.php"
      provides: "Gateway helpers and order-meta persistence for the expanded attempt contract"
      contains: "_vinti4_fingerprint"
  key_links:
    - from: "includes/class-vinti4-request-builder.php"
      to: "includes/class-wc-gateway-vinti4.php"
      via: "gateway-driven language and callback URL helpers used during attempt assembly"
      pattern: "build_payment_attempt"
    - from: "includes/class-wc-gateway-vinti4.php"
      to: "includes/class-vinti4-request-builder.php"
      via: "process_payment stores every canonical attempt field, including `FingerPrint`, as order meta for the redirect handoff"
      pattern: "update_meta_data.*_vinti4_(language_messages|url_merchant_response|is_3dsec|timestamp|fingerprint|fingerprint_version)"
---

<objective>
Expand the canonical payment-attempt builder so Phase 9 has one server-side source of truth for the SISP middleware contract before any redirect HTML is rendered.

Purpose: The live blocker is a request-shape problem, not just a missing locale toggle. This plan fixes the data contract at the source so later rendering and verification use the same persisted attempt fields every time.

Output: Updated request builder output, gateway-level language/callback helpers, and stored order meta for all request fields needed by the SISP middleware and later redirect rendering.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fingerprint-request-builder/03-02-SUMMARY.md
@.planning/phases/04-payment-redirect-flow/04-02-SUMMARY.md
@includes/class-vinti4-request-builder.php
@includes/class-wc-gateway-vinti4.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codify the full SISP request contract in the canonical attempt builder</name>
  <files>includes/class-vinti4-request-builder.php, includes/class-wc-gateway-vinti4.php</files>
  <action>Read the current builder and gateway first, then expand `Vinti4_Request_Builder::build_payment_attempt()` so it returns every field Phase 9 needs for the hosted middleware request from one canonical array. Keep the existing fingerprint algorithm untouched unless a field name/casing fix is required for transport only.

Implement gateway helpers as needed in `includes/class-wc-gateway-vinti4.php` so the builder can derive: `languageMessages`, `urlMerchantResponse`, `is3DSec`, canonical `timeStamp`, `FingerPrint`, and `FingerPrintVersion` (or the exact casing required by the middleware contract already captured in phase notes). Use store locale / gateway setting fallback for language resolution: prefer a real WordPress or WooCommerce locale when it cleanly maps to `pt` or `en`, otherwise fall back to the saved gateway setting, then to `pt`.

Keep field naming explicit and contract-oriented. Do not keep using the old `lang` transport field as the source of truth. Do not add raw `posAuthCode` to the returned attempt array.</action>
  <verify>`php -l includes/class-vinti4-request-builder.php` and `php -l includes/class-wc-gateway-vinti4.php` succeed; code review confirms `build_payment_attempt()` now returns `languageMessages`, `urlMerchantResponse`, `is3DSec`, `timeStamp`, `FingerPrint`, and `FingerPrintVersion` with exact transport names/casing</verify>
  <done>`build_payment_attempt()` returns the expanded request contract, including `FingerPrint`, and the gateway exposes any helper methods needed to resolve callback URL and language consistently</done>
</task>

<task type="auto">
  <name>Task 2: Persist the expanded attempt contract on the order for redirect rendering</name>
  <files>includes/class-wc-gateway-vinti4.php</files>
  <action>Update `process_payment()` so every canonical request field from Task 1 is saved as order meta before redirect. Persist the new language, callback URL, 3DS flag, timestamp casing, `FingerPrint`, and fingerprint-version values under explicit `_vinti4_*` keys that are easy for the redirect renderer to consume without recomputing anything.

Persist a single canonical handoff list that Plan 09-02 can consume directly: `_vinti4_language_messages`, `_vinti4_url_merchant_response`, `_vinti4_is_3dsec`, `_vinti4_timestamp`, `_vinti4_fingerprint`, and `_vinti4_fingerprint_version`, plus any existing browser-safe identifiers already required by the redirect flow. Preserve the current attempt storage pattern: build once, store once, redirect once. Do not store or log raw `posAuthCode`. If the existing payment-attempt log line needs updating, keep masking behavior and only log non-secret transport fields.</action>
  <verify>`php -l includes/class-wc-gateway-vinti4.php` succeeds; code review shows `process_payment()` stores `_vinti4_language_messages`, `_vinti4_url_merchant_response`, `_vinti4_is_3dsec`, `_vinti4_timestamp`, `_vinti4_fingerprint`, and `_vinti4_fingerprint_version` directly from the canonical attempt array</verify>
  <done>Order meta contains the full browser-safe redirect handoff contract, including `FingerPrint`, and the gateway no longer depends on ad hoc field reconstruction at render time</done>
</task>

</tasks>

<verification>
1. `build_payment_attempt()` returns the middleware language, callback URL, 3DS flag, `FingerPrint`, fingerprint version, and timestamp transport field in addition to the existing payment identifiers
2. `process_payment()` persists `_vinti4_language_messages`, `_vinti4_url_merchant_response`, `_vinti4_is_3dsec`, `_vinti4_timestamp`, `_vinti4_fingerprint`, and `_vinti4_fingerprint_version` before building the redirect URL
3. Plan 09-02 verification can assert the exact `_vinti4_*` keys persisted here are the ones consumed by `Vinti4_Redirect_Form::render()`
4. No new order meta key or log entry stores raw `posAuthCode`
</verification>

<success_criteria>
- Canonical attempt data fully represents the SISP middleware contract from server-side code
- Language selection is deterministic and maps to the field SISP actually validates
- Redirect rendering can consume stored meta only, including persisted `FingerPrint`, without re-reading secrets from settings or recomputing transport fields
</success_criteria>

<output>
After completion, create `.planning/phases/09-sisp-request-language-fix/09-01-SUMMARY.md`
</output>
