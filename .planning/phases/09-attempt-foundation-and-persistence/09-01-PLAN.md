---
phase: 09-attempt-foundation-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-vinti4-attempt-store.php
  - vinti4.php
  - tests/bootstrap.php
  - tests/Test_Attempt_Store.php
autonomous: true

must_haves:
  truths:
    - "Creating a new attempt for an order preserves all prior attempts without overwriting historical records"
    - "Attempt history can be listed for an order in deterministic chronological order (oldest to newest)"
    - "Latest-attempt projection remains available for existing redirect/callback code paths during migration"
  artifacts:
    - path: "includes/class-vinti4-attempt-store.php"
      provides: "Append-only persistence and retrieval API for order-scoped payment attempts"
      contains: "append_attempt"
    - path: "vinti4.php"
      provides: "Bootstrap loading for the attempt store class"
      contains: "class-vinti4-attempt-store.php"
    - path: "tests/Test_Attempt_Store.php"
      provides: "Regression coverage for append-only writes and chronological reads"
      contains: "test_append_attempt"
  key_links:
    - from: "includes/class-vinti4-attempt-store.php"
      to: "WooCommerce order meta"
      via: "_vinti4_attempt_history append-only writes"
      pattern: "_vinti4_attempt_history"
    - from: "tests/Test_Attempt_Store.php"
      to: "includes/class-vinti4-attempt-store.php"
      via: "assertions for non-overwrite behavior and sort order"
      pattern: "get_attempts"
---

<objective>
Establish the attempt persistence foundation so one WooCommerce order can safely keep multiple payment attempts as immutable history.

Purpose: Phase 9 requires attempt-level state that survives retries and partial-payment workflows without destructive writes to prior context.

Output: A dedicated attempt store with append-only write behavior, chronological read behavior, and bootstrap/test wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fingerprint-request-builder/03-02-SUMMARY.md
@.planning/phases/04-payment-redirect-flow/04-01-SUMMARY.md
@includes/class-wc-gateway-vinti4.php
@includes/class-vinti4-request-builder.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create append-only order attempt store</name>
  <files>includes/class-vinti4-attempt-store.php</files>
  <action>Create `Vinti4_Attempt_Store` as the single persistence boundary for payment attempts.

Implement public static methods to append and read attempts from order meta using one canonical history key (for example `_vinti4_attempt_history`). Enforce append-only semantics: each write pushes a new attempt record and never mutates or deletes prior records. Include deterministic sorting metadata (`created_at_gmt` and/or monotonic sequence) so chronological enumeration is stable.

Include a compatibility projection helper that can mirror the newest attempt into existing `_vinti4_*` single-attempt keys used by current redirect/callback code, but do not let that projection alter the stored history collection.</action>
  <verify>`php -l includes/class-vinti4-attempt-store.php` succeeds and code review confirms history writes append new records without replacing prior entries</verify>
  <done>Attempt store exposes append/read/projection behavior with explicit append-only guarantees</done>
</task>

<task type="auto">
  <name>Task 2: Wire attempt store into runtime and test bootstrap</name>
  <files>vinti4.php, tests/bootstrap.php</files>
  <action>Load the new attempt store class in plugin bootstrap and PHPUnit bootstrap so execution and tests resolve the class without manual includes.

Keep include order aligned with existing dependency flow (formatting/fingerprint/request builder before gateway runtime usage). Do not remove or reorder unrelated includes in ways that could regress v1.0 behavior.</action>
  <verify>`php -l vinti4.php` and `php -l tests/bootstrap.php` both succeed</verify>
  <done>Attempt store is autoloaded in both plugin runtime and test runtime using established include patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence regression tests for append-only and chronological retrieval</name>
  <files>tests/Test_Attempt_Store.php</files>
  <action>Add focused PHPUnit tests that simulate multiple attempt appends for one order and verify:
- earlier attempt payloads remain unchanged after later appends
- retrieval returns attempts in chronological order
- latest-attempt projection updates legacy `_vinti4_*` keys without truncating history.

Use lightweight WC_Order stubs consistent with current test style in `tests/bootstrap.php`; avoid integration-style test complexity.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Attempt_Store"` passes</verify>
  <done>Automated tests prove attempt persistence is append-only and chronologically enumerable</done>
</task>

</tasks>

<verification>
1. Attempt history meta stores multiple records for a single order without replacing earlier records
2. Retrieval API returns attempts sorted chronologically for deterministic iteration
3. Legacy `_vinti4_*` projection can coexist with append-only history
4. `vendor/bin/phpunit --filter "Test_Attempt_Store"` passes
</verification>

<success_criteria>
- ATT-04 persistence foundation exists: no destructive overwrite of prior attempts
- Attempt history can be enumerated chronologically from a dedicated store API
- Existing v1.0 redirect/callback consumers can keep reading latest projection during migration
</success_criteria>

<output>
After completion, create `.planning/phases/09-attempt-foundation-and-persistence/09-01-SUMMARY.md`
</output>
