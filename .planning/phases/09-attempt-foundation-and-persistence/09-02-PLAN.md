---
phase: 09-attempt-foundation-and-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - includes/class-vinti4-attempt-factory.php
  - includes/class-vinti4-request-builder.php
  - includes/class-wc-gateway-vinti4.php
  - vinti4.php
  - tests/bootstrap.php
  - tests/Test_Attempt_Factory.php
  - tests/Test_Request_Builder.php
autonomous: true

must_haves:
  truths:
    - "Each newly created attempt gets unique merchantRef and merchantSession values even when attempts are created close together"
    - "Request fingerprint generation is bound to the specific attempt amount/context rather than implicitly reusing stale order-level values"
    - "Attempt creation path appends to history and never mutates previously recorded attempts"
  artifacts:
    - path: "includes/class-vinti4-attempt-factory.php"
      provides: "Canonical attempt creation service for checkout/admin-triggered paths"
      contains: "create_attempt"
    - path: "includes/class-vinti4-request-builder.php"
      provides: "Attempt-context-aware request builder using explicit amount/context inputs"
      contains: "build_payment_attempt"
    - path: "includes/class-wc-gateway-vinti4.php"
      provides: "Checkout path integration that persists attempts through append-only storage"
      contains: "process_payment"
  key_links:
    - from: "includes/class-vinti4-attempt-factory.php"
      to: "includes/class-vinti4-request-builder.php"
      via: "factory delegates canonical request payload creation with explicit attempt context"
      pattern: "build_payment_attempt"
    - from: "includes/class-wc-gateway-vinti4.php"
      to: "includes/class-vinti4-attempt-store.php"
      via: "new attempt is appended to order history then projected for legacy consumers"
      pattern: "append_attempt"
---

<objective>
Introduce attempt-scoped generation so checkout and future admin actions can create new payment attempts with unique identifiers and amount-bound fingerprints while preserving historical attempts.

Purpose: This converts attempt creation from single-record overwrite behavior to reusable attempt-factory behavior required by ATT-01/ATT-02/ATT-03.

Output: Attempt factory service, request-builder support for explicit attempt context/amount, and gateway integration that appends attempts to history.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-attempt-foundation-and-persistence/09-01-SUMMARY.md
@includes/class-vinti4-attempt-store.php
@includes/class-vinti4-request-builder.php
@includes/class-wc-gateway-vinti4.php
@includes/functions-vinti4-formatting.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical attempt factory for order + amount context</name>
  <files>includes/class-vinti4-attempt-factory.php, includes/class-vinti4-request-builder.php, vinti4.php</files>
  <action>Create `Vinti4_Attempt_Factory` as the single service that generates a payment attempt for a given order and requested amount.

Factory contract must accept explicit amount/context input (for future partial requests) and produce a complete attempt payload (merchantRef, merchantSession, fingerprint, purchase request, metadata). Update `Vinti4_Request_Builder` as needed so fingerprint inputs come from the attempt context amount instead of always deriving from `$order->get_total()`.

Guarantee per-attempt uniqueness for `merchantRef` and `merchantSession` by combining timestamp with attempt-level entropy (not second-only timestamps). Preserve merchantRef parse compatibility (`WC{order_id}-...`) so existing order lookup behavior continues to work.</action>
  <verify>`php -l includes/class-vinti4-attempt-factory.php` and `php -l includes/class-vinti4-request-builder.php` succeed; code review confirms amount passed to fingerprint builder is attempt-scoped</verify>
  <done>Attempt factory can generate unique, amount-bound attempt payloads from explicit order/amount/context input</done>
</task>

<task type="auto">
  <name>Task 2: Integrate process_payment with append-only attempt persistence</name>
  <files>includes/class-wc-gateway-vinti4.php</files>
  <action>Refactor `process_payment()` to create attempts through `Vinti4_Attempt_Factory`, then persist via `Vinti4_Attempt_Store::append_attempt()` and latest-projection helper from Plan 09-01.

Maintain current checkout contract (`result`/`redirect`) and keep redirect-form compatibility by preserving required `_vinti4_*` latest-attempt keys through projection. Do not directly overwrite attempt history arrays from gateway code; all persistence must flow through the store abstraction.

Expose a reusable creation method in the gateway (or adjacent service entry point) that accepts a `WC_Order` and amount so Phase 10 admin flow can invoke attempt creation without re-running checkout internals.</action>
  <verify>`php -l includes/class-wc-gateway-vinti4.php` succeeds and code review confirms process_payment appends history instead of replacing prior attempt records</verify>
  <done>Checkout attempt creation uses factory + append-only storage and leaves prior attempts intact</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for uniqueness and amount-bound fingerprint context</name>
  <files>tests/bootstrap.php, tests/Test_Attempt_Factory.php, tests/Test_Request_Builder.php</files>
  <action>Add PHPUnit coverage that creates multiple attempts for the same order and verifies:
- each attempt has distinct `merchantRef` and `merchantSession`
- changing requested amount changes fingerprint input context and output
- generated attempts can be appended and enumerated without data loss.

Extend existing `Test_Request_Builder` only where needed to avoid duplicating assertions already covered by the new factory-focused suite. Keep tests deterministic by controlling entropy inputs via test doubles/mocks.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Attempt_Factory|Test_Request_Builder"` passes</verify>
  <done>Automated tests lock in ATT-02 and ATT-03 behaviors and guard against regression to single-attempt overwrite patterns</done>
</task>

</tasks>

<verification>
1. Repeated attempt generation for one order yields distinct merchantRef/session values
2. Fingerprint generation reflects per-attempt amount/context, not stale order-level defaults
3. process_payment persists attempts via append-only history path
4. `vendor/bin/phpunit --filter "Test_Attempt_Factory|Test_Request_Builder"` passes
</verification>

<success_criteria>
- ATT-01 foundation exists: code path for creating new attempts on existing orders is reusable outside checkout internals
- ATT-02 satisfied: uniqueness guarantees are enforced for merchantRef and merchantSession
- ATT-03 satisfied: fingerprint binds to attempt amount/context
- Phase 10 can consume this foundation to build admin partial-request UX
</success_criteria>

<output>
After completion, create `.planning/phases/09-attempt-foundation-and-persistence/09-02-SUMMARY.md`
</output>
