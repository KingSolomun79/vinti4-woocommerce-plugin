---
phase: 10-woocommerce-compatibility-fix
plan: 01
type: execute
wave: 1
depends_on:
  - 09-03
files_modified:
  - vinti4.php
  - includes/class-vinti4-feature-compatibility.php
autonomous: true

must_haves:
  truths:
    - "WooCommerce feature compatibility is declared before WooCommerce initializes feature gating so the plugin is not flagged as incompatible"
    - "Compatibility declarations are resilient on older WooCommerce versions and never trigger fatals when utility classes are unavailable"
    - "Declared support aligns with what the plugin actually implements today (HPOS-safe metadata and checkout blocks integration)"
  artifacts:
    - path: "includes/class-vinti4-feature-compatibility.php"
      provides: "Single source of truth for Vinti4 feature declarations"
      contains: "declare_compatibility"
    - path: "vinti4.php"
      provides: "Bootstrap hook wiring for WooCommerce feature declarations"
      contains: "before_woocommerce_init"
  key_links:
    - from: "vinti4.php"
      to: "includes/class-vinti4-feature-compatibility.php"
      via: "bootstrap hook invokes centralized declaration helper"
      pattern: "vinti4_declare_woocommerce_compatibility"
    - from: "includes/class-vinti4-feature-compatibility.php"
      to: "WooCommerce FeaturesUtil"
      via: "defensive declaration calls for enabled modern features"
      pattern: "declare_compatibility"
---

<objective>
Add robust WooCommerce feature compatibility declarations so WooCommerce no longer marks the plugin as incompatible with currently enabled features.

Purpose: The current admin warning blocks confidence in production readiness. This plan implements the bootstrap-level compatibility contract WooCommerce expects for modern features.

Output: Dedicated compatibility declaration helper plus plugin bootstrap wiring that declares support for the active WooCommerce feature set this gateway already supports.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-checkout-block-support/06-01-SUMMARY.md
@vinti4.php
@includes/class-wc-gateway-vinti4.php
@includes/class-wc-vinti4-blocks-support.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a centralized WooCommerce feature compatibility declaration helper</name>
  <files>includes/class-vinti4-feature-compatibility.php</files>
  <action>Create `Vinti4_Feature_Compatibility` as a small static helper with one public method to declare feature support against WooCommerce `FeaturesUtil`.

Declare support only for features the plugin already implements and uses in production. At minimum include `custom_order_tables` and `cart_checkout_blocks`, and keep the declaration list explicit so future additions are deliberate.

Use defensive guards (`class_exists` and `method_exists`) so no fatal occurs when WooCommerce internals differ by version. Keep plugin-file reference anchored to `VINTI4_PLUGIN_FILE` (or an equivalent explicit plugin bootstrap path) so declarations are tied to the correct plugin identity.</action>
  <verify>`php -l includes/class-vinti4-feature-compatibility.php` succeeds; code review confirms declaration calls are explicit, guarded, and plugin-file aware</verify>
  <done>A dedicated helper exists that declares Vinti4 feature compatibility safely and predictably</done>
</task>

<task type="auto">
  <name>Task 2: Wire declarations into plugin bootstrap at the correct lifecycle hook</name>
  <files>vinti4.php</files>
  <action>Load the new compatibility helper in plugin bootstrap and register a `before_woocommerce_init` callback that runs declarations exactly once during load.

Keep the callback minimal and side-effect free: declaration only, no gateway instantiation, no checkout logic. Ensure the declaration path works even when WooCommerce is active but some optional feature utility APIs are absent.

Do not regress existing bootstrap behavior for missing WooCommerce notice handling, gateway registration, or rewrite endpoint setup.</action>
  <verify>`php -l vinti4.php` succeeds; code review confirms compatibility declaration hook is registered before WooCommerce init without changing existing payment bootstrap flow</verify>
  <done>Bootstrap registers compatibility declarations at the expected WooCommerce hook and keeps all existing initialization behavior intact</done>
</task>

</tasks>

<verification>
1. `before_woocommerce_init` hook includes a Vinti4 compatibility declaration callback
2. Declarations are guarded against missing WooCommerce utility classes/methods
3. Feature list matches currently implemented capabilities (`custom_order_tables`, `cart_checkout_blocks`)
4. No unrelated bootstrap flow changes are introduced
</verification>

<success_criteria>
- WooCommerce can recognize the plugin as compatible with enabled modern features
- Declarations are centralized and maintainable instead of scattered hook snippets
- Plugin remains safe on WooCommerce versions where some feature utility APIs differ
</success_criteria>

<output>
After completion, create `.planning/phases/10-woocommerce-compatibility-fix/10-01-SUMMARY.md`
</output>
