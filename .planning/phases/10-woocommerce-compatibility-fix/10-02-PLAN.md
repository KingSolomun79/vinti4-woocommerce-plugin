---
phase: 10-woocommerce-compatibility-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/bootstrap.php
  - tests/Test_Feature_Compatibility.php
  - includes/class-vinti4-admin-test-panel.php
autonomous: true

must_haves:
  truths:
    - "Automated tests fail if WooCommerce compatibility declarations are removed, renamed, or wired to the wrong plugin file"
    - "The admin diagnostics surface declaration status so support can quickly identify feature-compatibility drift"
    - "Coverage asserts both declaration content and defensive behavior when WooCommerce feature utilities are unavailable"
  artifacts:
    - path: "tests/Test_Feature_Compatibility.php"
      provides: "Regression tests for declaration map, hook behavior, and defensive guards"
      contains: "Vinti4_Feature_Compatibility"
    - path: "tests/bootstrap.php"
      provides: "Feature utility stubs/spies for compatibility declaration assertions"
      contains: "FeaturesUtil"
    - path: "includes/class-vinti4-admin-test-panel.php"
      provides: "Admin diagnostic signal for compatibility declarations"
      contains: "compatibility"
  key_links:
    - from: "tests/Test_Feature_Compatibility.php"
      to: "includes/class-vinti4-feature-compatibility.php"
      via: "asserts declared features and plugin-file path passed into FeaturesUtil"
      pattern: "declare_compatibility"
    - from: "includes/class-vinti4-admin-test-panel.php"
      to: "includes/class-vinti4-feature-compatibility.php"
      via: "admin self-test reads declaration intent/result for operator visibility"
      pattern: "feature"
---

<objective>
Lock Phase 10 behavior down with automated regression coverage and a visible admin diagnostic check.

Purpose: The warning can silently return when hooks, feature names, or plugin-file references drift. This plan adds guards that keep compatibility declarations trustworthy over time.

Output: PHPUnit coverage for declaration logic plus a self-test panel check that reports compatibility declaration health in admin.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-testing-certification-prep/08-01-SUMMARY.md
@.planning/phases/10-woocommerce-compatibility-fix/10-01-SUMMARY.md
@tests/bootstrap.php
@includes/class-vinti4-admin-test-panel.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PHPUnit regression tests for feature declarations</name>
  <files>tests/bootstrap.php, tests/Test_Feature_Compatibility.php</files>
  <action>Extend test bootstrap with a minimal `FeaturesUtil` spy/stub surface (or equivalent test doubles) so declaration calls can be asserted without a full WooCommerce runtime.

Create `tests/Test_Feature_Compatibility.php` with focused assertions that cover:
- expected feature slugs are declared (`custom_order_tables`, `cart_checkout_blocks`)
- declarations use the plugin bootstrap file reference, not a derived or incorrect path
- helper returns safely when utility classes/methods are absent (no fatal)
- bootstrap hook wiring calls declaration logic once at the expected lifecycle stage.

Prefer explicit assertions over snapshots; failures should show exactly which feature or declaration call regressed.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Feature_Compatibility"` passes</verify>
  <done>Compatibility declaration behavior is covered by deterministic automated tests</done>
</task>

<task type="auto">
  <name>Task 2: Expose declaration health in the admin self-test panel</name>
  <files>includes/class-vinti4-admin-test-panel.php</files>
  <action>Add or extend a self-test that reports whether compatibility declarations are configured as expected.

Keep the check lightweight and operator-friendly: include feature names and pass/fail detail that helps support quickly explain why WooCommerce may still show compatibility warnings.

Do not add expensive runtime scans or behavior that depends on making external network requests.</action>
  <verify>`php -l includes/class-vinti4-admin-test-panel.php` succeeds; admin test output includes a compatibility declaration check with actionable detail</verify>
  <done>Admin diagnostics make compatibility declaration state visible without reading source code</done>
</task>

<task type="auto">
  <name>Task 3: Run the focused verification loop for Phase 10 code paths</name>
  <files>tests/Test_Feature_Compatibility.php, includes/class-vinti4-feature-compatibility.php</files>
  <action>Run targeted tests for compatibility declarations first, then run full PHPUnit to ensure no regressions outside Phase 10.

If test bootstrap stubs conflict with existing suites, resolve the fixture isolation issue rather than weakening declaration assertions.</action>
  <verify>`vendor/bin/phpunit --filter "Test_Feature_Compatibility"` passes, then `vendor/bin/phpunit` passes</verify>
  <done>Phase 10 declaration behavior is regression-protected and full suite remains green</done>
</task>

</tasks>

<verification>
1. `Test_Feature_Compatibility` asserts feature slugs and plugin-file argument passed to declaration calls
2. Defensive-path tests prove safe behavior when feature utility internals are unavailable
3. Admin self-test panel reports compatibility declaration status
4. `vendor/bin/phpunit` passes with new coverage included
</verification>

<success_criteria>
- Compatibility declaration regressions are detected automatically
- Support/debug workflows can confirm declaration status from admin diagnostics
- No existing PHPUnit coverage is broken by the new declaration tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-woocommerce-compatibility-fix/10-02-SUMMARY.md`
</output>
