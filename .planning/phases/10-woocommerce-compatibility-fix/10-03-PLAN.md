---
phase: 10-woocommerce-compatibility-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Real WordPress admin no longer shows the WooCommerce incompatible-features warning for Vinti4 when declared features are enabled"
    - "Checkout still works in both shortcode and block flows after declarations are added"
    - "No new PHP warnings/notices are introduced during plugin load from compatibility declaration hooks"
  artifacts:
    - path: "vinti4.php"
      provides: "Live bootstrap hook for WooCommerce compatibility declarations"
      contains: "before_woocommerce_init"
    - path: "includes/class-vinti4-feature-compatibility.php"
      provides: "Feature declaration execution path used in production"
      contains: "custom_order_tables"
  key_links:
    - from: "WordPress admin Plugins page"
      to: "WooCommerce enabled feature set"
      via: "compatibility warning state after plugin activation"
      pattern: "incompatible with currently enabled WooCommerce features"
---

<objective>
Complete the Phase 10 live verification gate in a real WordPress environment and confirm the incompatibility warning is resolved end-to-end.

Purpose: Compatibility declarations are only done when WooCommerce recognizes them in the real admin environment. This checkpoint proves the warning is actually gone for operators.

Output: Human-verified proof that WooCommerce no longer flags Vinti4 as incompatible and that checkout entry points remain healthy.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-woocommerce-compatibility-fix/10-01-SUMMARY.md
@.planning/phases/10-woocommerce-compatibility-fix/10-02-SUMMARY.md
@vinti4.php
@includes/class-vinti4-feature-compatibility.php
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WooCommerce feature compatibility declarations, regression coverage, and admin diagnostics for declaration health</what-built>
  <how-to-verify>
1. In the WordPress sandbox, activate WooCommerce features currently enabled in your target environment (at least HPOS/custom order tables and checkout blocks if in use).
2. Activate or reload the Vinti4 plugin and open the Plugins screen and WooCommerce status/admin notices area.
3. Confirm the warning `You are viewing active plugins that are incompatible with currently enabled WooCommerce features.` no longer lists Vinti4.
4. Open WooCommerce settings for Vinti4 and run the admin self-test panel check related to feature compatibility; record pass/fail output.
5. Run a smoke checkout in classic shortcode checkout and block checkout (if enabled) to ensure declarations did not regress availability or redirect behavior.
6. If any warning remains, capture the exact feature slug WooCommerce reports as incompatible and the environment feature toggles so follow-up can target the missing declaration precisely.</how-to-verify>
  <resume-signal>Type "approved" if warnings are cleared and smoke checks pass, or reply with the exact warning text and reported feature slug.</resume-signal>
</task>

</tasks>

<verification>
1. WooCommerce admin no longer flags Vinti4 as incompatible with enabled features
2. Admin diagnostics confirm declaration health
3. Basic classic and block checkout availability remains intact
</verification>

<success_criteria>
- The original compatibility warning is resolved in real WordPress admin
- No regressions are observed in checkout method availability after declaration changes
- The phase can close with evidence tied to real feature toggles, not assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/10-woocommerce-compatibility-fix/10-03-SUMMARY.md`
</output>
