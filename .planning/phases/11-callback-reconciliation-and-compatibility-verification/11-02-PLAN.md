---
phase: 11-callback-reconciliation-and-compatibility-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - includes/class-vinti4-callback-handler.php
  - includes/class-vinti4-redirect-form.php
  - includes/class-wc-gateway-vinti4.php
  - tests/Test_Callback_Handler.php
autonomous: true

must_haves:
  truths:
    - Existing hosted 3DS redirect flow for SISP card payments remains functional after multi-attempt changes
    - Sandbox 3DS test card can complete a partial-attempt payment path in test mode
    - Logs include attempt ID, amount, merchantRef, and callback outcome per attempt
    - Logs clearly distinguish invalid reference vs invalid fingerprint vs duplicate callback in multi-attempt flows
  artifacts:
    - path: includes/class-vinti4-callback-handler.php
      provides: Enhanced diagnostics logging with attempt-scoped context
      exports: ["log_callback_outcome", "log_validation_failure", "log_duplicate_callback"]
    - path: includes/class-vinti4-redirect-form.php
      provides: Payment form rendering with attempt validation
      exports: ["render_payment_form", "validate_order_key"]
    - path: tests/Test_Callback_Handler.php
      provides: Compatibility verification test coverage
      contains: ["test_legacy_callback_flow", "test_sandbox_card_flow", "test_multi_attempt_logging"]
  key_links:
    - from: includes/class-vinti4-callback-handler.php
      to: includes/class-vinti4-logger.php
      via: attempt-scoped diagnostics logging
      pattern: Vinti4_Logger::log.*attempt_id.*merchantRef.*outcome
    - from: includes/class-vinti4-redirect-form.php
      to: includes/class-vinti4-attempt-store.php
      via: attempt validation for form rendering
      pattern: Vinti4_Attempt_Store::(get_attempts|find_attempt_by_merchant_ref)
    - from: includes/class-wc-gateway-vinti4.php
      to: includes/class-vinti4-redirect-form.php
      via: payment form generation for checkout
      pattern: render_payment_form.*order.*attempt
---
<objective>
Add compatibility verification, sandbox card flow checks, and enhanced multi-attempt diagnostics logging.

Purpose: After implementing attempt-level reconciliation, we need to verify backward compatibility with existing card flows, add comprehensive logging for debugging multi-attempt scenarios, and ensure sandbox testing works correctly.

Output: Enhanced logging system that logs attempt-scoped diagnostics, verified backward compatibility with legacy orders, and test coverage for sandbox card flows.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@includes/class-vinti4-callback-handler.php
@includes/class-vinti4-redirect-form.php
@includes/class-vinti4-logger.php
@includes/class-wc-gateway-vinti4.php
@.planning/phases/11-callback-reconciliation-and-compatibility-verification/11-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Add enhanced attempt-scoped diagnostics logging to callback handler</name>
  <files>includes/class-vinti4-callback-handler.php</files>
  <action>
    1. Add static method log_callback_outcome(WC_Order $order, array $attempt, string $outcome, string $detail = ''): void:
       - Log entry includes: attempt_id, merchant_ref, amount, merchant_session, transaction_id (if success), outcome, detail
       - Use log level 'info' for successful callbacks, 'error' for failures, 'warning' for duplicates
       - Format: "Vinti4 callback: attempt={attempt_id} ref={merchant_ref} amount={amount} outcome={outcome} detail={detail}"

    2. Add static method log_validation_failure(WC_Order $order, array $attempt, string $failure_type, string $reason): void:
       - Called for each validation failure path (merchantRef not found, session mismatch, fingerprint mismatch, amount mismatch)
       - failure_type distinguishes between: 'invalid_reference', 'invalid_session', 'invalid_fingerprint', 'amount_mismatch', 'duplicate_callback'
       - Log includes attempt context and specific reason
       - Example: "Vinti4 validation failure: type={failure_type} attempt={attempt_id} ref={merchant_ref} reason={reason}"

    3. Update all validation failure paths to use new logging methods:
       - Replace direct Vinti4_Logger::log() calls with log_validation_failure() for structured output
       - Ensure merchantRef, attempt_id, and amount are always logged (even if null/empty)
       - Add timestamp to log entries for debugging

    4. Add static method log_duplicate_callback(WC_Order $order, array $attempt, string $processed_timestamp): void:
       - Called when idempotency check detects duplicate callback
       - Logs original processing time and current duplicate time
       - Format: "Vinti4 duplicate callback: attempt={attempt_id} ref={merchant_ref} originally_processed={processed_timestamp} duplicate_time={current_time}"

    5. Add callback entry and exit logging:
       - At start of handle(): log "Vinti4 callback received: order={order_id} merchant_ref={merchant_ref}"
       - At end of handle(): log "Vinti4 callback completed: order={order_id} attempt={attempt_id} status={final_status}"
       - Use log level 'debug' for entry/exit to avoid noise in production
  </action>
  <verify>
    Read the updated callback handler file and verify:
    - log_callback_outcome(), log_validation_failure(), log_duplicate_callback() methods exist
    - All validation paths use structured logging with failure_type
    - Logs include attempt_id, merchant_ref, amount, and transaction_id where applicable
    - Entry/exit logging exists at handle() start and end
  </verify>
  <done>
    Callback handler logs attempt-scoped diagnostics that distinguish between invalid reference, invalid session, invalid fingerprint, amount mismatch, and duplicate callback failures.
  </done>
</task>

<task type="auto">
  <name>Add backward compatibility verification for legacy callback flow</name>
  <files>includes/class-vinti4-callback-handler.php, tests/Test_Callback_Handler.php</files>
  <action>
    1. Enhance legacy fallback path in handle() method:
       - When find_attempt_by_merchant_ref() returns null AND _vinti4_attempt_history is empty:
         - Log "Legacy callback path detected: order {order_id} has no attempt history, using order-level validation"
         - Validate against _vinti4_merchant_ref order meta (existing behavior)
         - Validate against _vinti4_amount order meta (existing behavior)
         - Use existing order-level idempotency via _vinti4_callback_processed
         - Mark order as legacy: add order meta '_vinti4_is_legacy_order' = true
         - Log "Legacy order marked for diagnostics: order {order_id}"

    2. Add test case test_legacy_callback_flow() in tests/Test_Callback_Handler.php:
       - Create order without _vinti4_attempt_history meta
       - Set legacy meta keys: _vinti4_merchant_ref, _vinti4_amount
       - Simulate callback with matching legacy merchantRef
       - Assert callback is processed via legacy path
       - Assert _vinti4_is_legacy_order meta is set
       - Assert order completes successfully

    3. Add test case test_legacy_callback_invalid_ref() in tests/Test_Callback_Handler.php:
       - Create order without _vinti4_attempt_history meta
       - Simulate callback with non-matching merchantRef
       - Assert callback fails via legacy path
       - Assert diagnostic log indicates legacy path failure

    4. Add test case test_legacy_callback_idempotency() in tests/Test_Callback_Handler.php:
       - Create legacy order with _vinti4_callback_processed meta
       - Simulate duplicate callback
       - Assert callback is rejected as duplicate via legacy path
       - Assert log indicates duplicate detection
  </action>
  <verify>
    Read the updated callback handler file and test file and verify:
    - Legacy fallback path logs "Legacy callback path detected"
    - Legacy orders are marked with _vinti4_is_legacy_order meta
    - Test cases cover legacy flow, invalid ref, and idempotency
    - Tests assert correct log messages for legacy path
  </verify>
  <done>
    Legacy callback flow is preserved with diagnostic logging and test coverage for backward compatibility verification.
  </done>
</task>

<task type="auto">
  <name>Add sandbox card flow verification and multi-attempt logging tests</name>
  <files>tests/Test_Callback_Handler.php</files>
  <action>
    1. Add test case test_sandbox_card_flow_single_attempt() in tests/Test_Callback_Handler.php:
       - Create order with single attempt (simulating checkout)
       - Set attempt to reflect sandbox test card: pan=4012001037141112, exp=12/25, cvv2=123
       - Simulate successful callback with message_type='8'
       - Assert attempt is marked as completed
       - Assert order is fully completed (payment_complete called)
       - Assert logs show "Order fully paid: multiple attempts completed" (even for single attempt)

    2. Add test case test_sandbox_card_flow_partial_payment() in tests/Test_Callback_Handler.php:
       - Create order with partial attempt (50% of order total)
       - Simulate successful callback for partial attempt
       - Assert attempt is marked as completed
       - Assert order status is 'processing' (not 'completed')
       - Assert _vinti4_paid_total equals partial amount
       - Assert _vinti4_outstanding_total equals remaining balance
       - Assert order note includes partial payment details
       - Assert logs show "Partial payment: order {order_id} now partially paid"

    3. Add test case test_multi_attempt_full_payment() in tests/Test_Callback_Handler.php:
       - Create order with first partial attempt (50%)
       - Simulate successful callback for first attempt
       - Create second partial attempt (remaining 50%)
       - Simulate successful callback for second attempt
       - Assert both attempts are marked as completed
       - Assert order is fully completed (payment_complete called after second attempt)
       - Assert _vinti4_paid_total equals order total
       - Assert _vinti4_outstanding_total equals 0
       - Assert logs show both callbacks with attempt-scoped context

    4. Add test case test_multi_attempt_distinguishes_failure_types() in tests/Test_Callback_Handler.php:
       - Test invalid_reference: callback with non-existent merchantRef
       - Test invalid_fingerprint: callback with mismatched fingerprint
       - Test duplicate_callback: same callback received twice
       - Assert each failure type has distinct log message with failure_type
       - Assert log messages include attempt_id, merchant_ref, and specific reason
  </action>
  <verify>
    Read the test file and verify:
    - test_sandbox_card_flow_single_attempt() exists and validates single-attempt completion
    - test_sandbox_card_flow_partial_payment() exists and validates partial payment handling
    - test_multi_attempt_full_payment() exists and validates multi-attempt full payment
    - t
est_multi_attempt_distinguishes_failure_types() exists and validates distinct logging
    - All tests assert correct log messages and attempt-scoped context
  </verify>
  <done>
    Sandbox card flow verification tests cover single attempt, partial payments, multi-attempt full payment, and distinct failure type logging.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Read the updated callback handler file and verify:
   - All validation paths use structured logging with failure_type
   - Legacy fallback path preserves backward compatibility
   - Logs include attempt_id, merchant_ref, amount, and transaction_id

2. Read the test file and verify:
   - Legacy flow tests cover success, invalid ref, and idempotency
   - Sandbox flow tests cover single attempt, partial payment, and multi-attempt
   - Failure type tests distinguish between different failure reasons

3. Verify backward compatibility:
   - Legacy orders without attempt history are processed via order-level validation
   - Existing card flows remain functional
</verification>

<success_criteria>
Phase 11 Plan 2 is complete when:
- Existing hosted 3DS redirect flow remains functional after multi-attempt changes
- Sandbox 3DS test card can complete partial-attempt payments in test mode
- Logs include attempt ID, amount, merchantRef, and callback outcome per attempt
- Logs clearly distinguish invalid reference vs invalid fingerprint vs duplicate callback
- Test coverage verifies backward compatibility and multi-attempt scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/11-callback-reconciliation-and-compatibility-verification/11-02-SUMMARY.md`
</output>
